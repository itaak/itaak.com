---
import { useLanguage } from "../../i18n/utils";
const { t } = useLanguage(Astro.url);
---

<nav>
    <div class="resume">
        <span>T. ( +33 (0)6 89 44 07 83 )</span><br />
        <span>E. ( contact@itaak.com )</span>
        <p>
            {t("nav.resume")}
        </p>
    </div>
    <ul>
        <slot />
    </ul>
</nav>

<style lang="scss">
    nav {
        display: grid;
        grid-template-columns: repeat(2, 1fr);

        ul {
            display: flex;
            flex-direction: column;
            align-items: end;
            gap: 8px;
        }
    }
</style>
